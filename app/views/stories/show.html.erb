<h1><%= @story.title %></h1>
<p><%= @story.content %></p>

<%= form_with(model: @story, method: :patch) do |form| %>
  <%= form.hidden_field :content, value: @story.content %>
  <p>L'histoire a été sauvegardée</p>
<% end %>
